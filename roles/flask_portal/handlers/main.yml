---
  - name: reload systemd
    ansible.builtin.systemd:
      daemon_reload: true

  - name: restart portal
    ansible.builtin.service:
      name: cdpni-portal
      state: restarted

  - name: reload nginx
    ansible.builtin.service:
      name: nginx
      state: reloaded
